# LLM API Configuration
# To enable real persona analysis, uncomment and set your OpenAI API key
# Without this, the app uses mock/demo data
# OPENAI_API_KEY=sk-...

# Optional: Configure which model to use (default: gpt-4o)
# LLM_MODEL_NAME=gpt-4o
